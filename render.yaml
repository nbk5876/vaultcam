services:
  - type: web
    name: vaultcam
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn vaultcam:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
